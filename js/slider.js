$(document).ready(function () {
  $('.slider').slick({
    dots: true,
    arrows: false,
    autoplay: true,
    sliderToShow: 3,
    autoplaySpeed: 2500,
    centerMode: false,
    speed: 600,
  });
});
